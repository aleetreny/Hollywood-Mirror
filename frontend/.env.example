# Optional: if omitted, production defaults to same-origin requests and
# local development defaults to http://localhost:8000.
VITE_API_BASE_URL="http://localhost:8000"
# Optional timeout for frontend requests in milliseconds.
VITE_API_TIMEOUT_MS="45000"
