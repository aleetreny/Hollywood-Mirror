# Local data pipeline and analysis environment.
# The production web app is client-only and uses the Node dependencies in frontend/.

numpy>=1.24,<2
pandas>=2.0
plotly>=5.15
pyarrow>=12.0
scikit-learn>=1.3
sentence-transformers>=2.2,<3
textblob>=0.18
torch>=2.0
tqdm>=4.65
umap-learn>=0.5
