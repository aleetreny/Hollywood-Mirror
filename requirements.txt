# Hollywood Mirror: dependencies
# Python >= 3.9 recommended

# Data and pipeline
pandas>=2.0
numpy>=1.24
tqdm>=4.65

# Embeddings (chunking + mean pooling; torch 2.2.x works on macOS)
torch>=2.0
sentence-transformers>=2.2,<3

# Persistence (Nxd matrix: 768 with mpnet, 384 with minilm)
pyarrow>=12.0

# Route 1: Quarto analysis (UMAP, visualization)
umap-learn>=0.5
plotly>=5.15
scikit-learn>=1.3

# Classical NLP for lexical metrics
textblob>=0.18

# Backend API (FastAPI)
fastapi>=0.115
uvicorn[standard]>=0.32
