# Hollywood Mirror — dependencias
# Python >= 3.9 recomendado

# Datos y pipeline
pandas>=2.0
numpy>=1.24
tqdm>=4.65

# Embeddings (chunking + mean pooling; torch 2.2.x OK en macOS)
torch>=2.0
sentence-transformers>=2.2,<3

# Persistencia (matriz N×d: 768 con mpnet, 384 con minilm)
pyarrow>=12.0

# Vía 1: Análisis Quarto (UMAP, visualización)
umap-learn>=0.5
plotly>=5.15
scikit-learn>=1.3

# Vía 2: App Streamlit
streamlit>=1.28
